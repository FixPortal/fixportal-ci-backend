<Project>
  <!-- Compiler warnings are build-breaking so they cannot accrue silently.
       Code style is enforced via the in-repo .editorconfig. -->
  <PropertyGroup>
    <Version>1.0.0</Version>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>
</Project>
